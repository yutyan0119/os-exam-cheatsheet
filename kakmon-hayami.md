# 2021
1. 仮想アドレスと仮想コアの話/特権命令
2. ページテーブルの話。コピーオンライト。論理ページ。特権命令。CFS
3. `pthread_mutex`, `pthread_cond`, `futex`, `__sync_bool_compare_and_swap`
4. ファイルの取り扱い方 `mmap`

# 2020
1. `mmap`, `malloc`, コピーオンライト, 論理ページ
2. vruntimeの仕組み
3. ファイルの取り扱い
4. 二分探査とそれの排他制御 `pthread_mutex`, `pthread_cond`

# 2019
1. 論理アドレス, TLB, MMU, シグナル, マイナーフォルト, メジャーフォルト, ヨウキュウジページング
2. バリア同期, スレッドセーフ, `pthread_mutex`, `pthread_cond`
3. binarysearchとmmap

# 2018
1. 特権, chmod, root, chown, euid
2. スレッドセーフ `pthread_mutex` `pthread_cond`など量が多い。lock/block
3. binarysearchとキャッシュなど

# 2017
1. ページテーブル, 論理アドレス, 特権命令, システムコール
2. 2018とほぼ同じような問題
3. mmapとキャッシュ

# 2016
1. 仮想記憶, 物理アドレス, 論理アドレス, MMU, TLB, mmap, ヨウキュウジページング
2. 周辺機器を保護する仕組み, 特権命令, 割り込み, vruntime
3. スレッドセーフティ

# 2015
1. mmapと確保されるメモリ量, 要求時ページング, copy on write
2. スレッドセーフティ
3. 仮想記憶とキャッシュ, 先読み

# 2014
1. ps ax, pthread_mutexとか, タイマ割り込み, vruntime
2. キャッシュ, ページ
3. dirty_bitの話（範囲外？）

# 2013
